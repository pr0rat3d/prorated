
-- Dead since the push-to-email migration — nothing writes to this anymore.
-- Same class as the 3 SECURITY DEFINER views dropped earlier this session.
drop table if exists public.push_subscriptions;
